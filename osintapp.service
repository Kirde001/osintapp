[Unit]
Description=Gunicorn instance
After=network.target

[Service]
User=root
WorkingDirectory=/root/osint/
Environment="PATH=/root/osint/venv/bin"
ExecStart=/root/osint/venv/bin/gunicorn -w 4 -b 127.0.0.1:5000 app:app --timeout 120

[Install]
WantedBy=multi-user.target